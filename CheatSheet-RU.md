# Шпаргалка: `float` (IEEE‑754, 32 бита) 💧

## 1. Что такое `float` 🧮

* Тип **числа с плавающей запятой**.
* Значение хранится примерно как:

  **value = (−1)^sign × 1.mantissa₂ × 2^e**

  где `1.mantissa₂` — двоичная мантисса (значащая часть), а `e` — целочисленная экспонента.
* Это **приближённое** представление: большинство десятичных дробей (0.1, 0.2, 0.3 …) нельзя представить точно.

---

## 2. Внутреннее устройство `float` 🧱

Всего **32 бита**:

* 1 бит — **sign** (знак: 0 = плюс, 1 = минус ➕➖)
* 8 бит — **exponent** (экспонента со смещением / bias = 127)
* 23 бита — **fraction** (дробная часть мантиссы)

Декодирование значения:

```text
value = (−1)^sign_bit × 1.fraction_bits₂ × 2^(exponent_bits − 127)
```

* `1.fraction_bits₂` — «скрытая единица» + 23 бита дроби.
* `exponent_bits` — беззнаковое значение, записанное в поле экспоненты.

---

## 3. Диапазон и точность 🎯

* Примерный диапазон по модулю: **от ~1.18×10⁻³⁸ до ~3.4×10³⁸**.
* Точность: примерно **6–7 десятичных значащих цифр**.

👉 Правило: `float` подходит для «примерных» реальных чисел, но не для денег и не для точной математики.

---

## 4. Особые значения ⚠️

Зарезервированные комбинации битов:

* **+0.0 / −0.0** — exponent = 0, fraction = 0, знак определяет +0 или −0.
* **+∞ / −∞** — exponent = 255 (все единицы), fraction = 0.
* **NaN** (Not a Number) — exponent = 255, fraction ≠ 0.

`NaN` появляется, когда результат математически «не имеет смысла», например `0.0f / 0.0f`.

---

## 5. Типичные грабли с `float` 🪤

1. **Сравнение через `==` обычно опасно**

   ```cpp
   float a = 0.1f + 0.2f;
   float b = 0.3f;

   // Плохо (из‑за округления может быть false):
   if (a == b) { /* ... */ }

   // Лучше:
   if (fabs(a - b) < 1e-6f) { /* ... */ }
   ```

   Многие десятичные дроби не представимы точно в двоичной форме → маленькие ошибки округления.

2. **Накопление ошибок** ⏱️

   При длинных вычислениях погрешности накапливаются.

3. **Переполнение и потеря значимости** 💥

   * Очень большие результаты → ±∞.
   * Вычитание почти равных чисел → потеря значащих цифр (cancellation).

---

## 6. `float` vs `double` 🆚

* `float`: 32 бита, ~6–7 значащих цифр.
* `double`: 64 бита, ~15–16 значащих цифр.

👉 В большинстве «серьёзных» вычислений используют `double`; `float` берут там, где важны скорость/память (графика, большие массивы, GPU и т.п.).

---

## 7. Связь с fixed‑point (`Fixed` из C++ Module 02) ⚖️

* `float`: мантисса + экспонента → **точка «плавает»**, расстояние между соседними представимыми числами зависит от масштаба.
* fixed‑point: одно целое + фиксированное количество дробных бит → **точка фиксирована**, шаг между соседними значениями постоянный (`1 / 2^fractionalBits`).

Приблизительная аналогия:

* `float` = научная нотация в базе 2.
* fixed‑point = целочисленная сетка с фиксированным шагом.

---

# Шпаргалка: как получить вид ±1.xxx₂ × 2^e 🔄

Это **нормализованная двоичная форма**, которую концептуально использует IEEE‑754.

## 1. Основная идея 💡

Для любого ненулевого конечного числа:

> **x = (±1.xxx₂) × 2^e**

где слева от двоичной точки всегда ровно одна `1` (нормализованный вид).

---

## 2. Алгоритм (математический взгляд) 📐

### Шаг 1. Определить знак

* Если `x < 0` → `sign = 1`, дальше работаем с `|x|`.
* Если `x ≥ 0` → `sign = 0`.

### Шаг 2. Перевести число в двоичную систему

Разбиваем число на целую и дробную части:

```text
x = N + f, где N — целая часть, f — дробная (0 ≤ f < 1)
```

1. **Целую часть N** переводим делением на 2 с накоплением остатков (читаем остатки снизу вверх → биты).
2. **Дробную часть f** переводим умножением на 2 и взятием целой части (0 или 1) как следующего бита после точки.

Получаем двоичную запись вида `…xxx.yyy₂`.

### Шаг 3. Нормализуем до 1.xxx₂

Двигаем двоичную точку так, чтобы слева осталась одна `1`.

* Пример (большее число):

  * `101.11₂ → 1.0111₂ × 2²`
  * Сдвинули точку **влево на 2 позиции** → экспонента `e = 2`.

* Пример (маленькое число):

  * `0.00101₂ → 1.01₂ × 2⁻³`
  * Сдвинули точку **вправо на 3 позиции** → экспонента `e = −3`.

Правило:

* Сдвиг **влево на k** → умножение на `2^k`.
* Сдвиг **вправо на k** → умножение на `2^(−k)` (то же, что деление на `2^k`).

Этот сдвиг компенсируем в экспоненте `e`.

### Шаг 4. Финальная нормализованная форма ✅

Записываем число как:

```text
x = (±1.mantissa₂) × 2^e
```

где `1.mantissa₂` — нормализованная мантисса, `e` — экспонента.

---

## 3. Переход к IEEE‑754 `float` 🧾

Когда у нас есть `x = (±1.mantissa₂) × 2^e`, можно собрать реальное представление `float`:

1. **Бит знака**:

   * 0, если `x ≥ 0`,
   * 1, если `x < 0`.

2. **Поле экспоненты (8 бит)**:

   * `exponent_bits = e + 127` (для 32‑битного float, bias = 127).

3. **Поле мантиссы (23 бита)**:

   * записываем только дробную часть мантиссы (биты после `1.`),
   * если битов меньше 23 → дополняем нулями,
   * если больше → округляем.

Формула для декодирования:

```text
value = (−1)^sign_bit × 1.fraction_bits₂ × 2^(exponent_bits − 127)
```

---

## 4. Мини‑пример (5.75) 🧊

1. Десятичное → двоичное:

   * 5.75₁₀ = 101.11₂
2. Нормализация:

   * `101.11₂ → 1.0111₂ × 2²` → мантисса = `1.0111₂`, экспонента `e = 2`.
3. Знак:

   * `sign_bit = 0` (число положительное).
4. Биты экспоненты:

   * `exponent_bits = e + 127 = 2 + 127 = 129` → `10000001₂`.
5. Биты мантиссы:

   * дробная часть = `0111`, дополняем до 23 бит: `01110000000000000000000`.

Итоговая раскладка IEEE‑754:

```text
sign      exponent        fraction
0         10000001        01110000000000000000000
```

Этот шаблон соответствует значению `5.75f` в памяти 💾.
